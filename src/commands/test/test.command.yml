name: test
class: TestCommand
description: 'Execute pentest on a Drupal/WordPress sites via xmlrpc'
options:
    - name: url
      type: string,
      description: 'The url to test'
    - name: path
      alias: p
      type: string
      description: 'The path to the xmlrpc.php file from the url'